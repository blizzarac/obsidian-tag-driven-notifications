{
  "name": "obsidian-tag-driven-notifications",
  "version": "1.0.0",
  "description": "Obsidian plugin for date-based notifications from tags and frontmatter fields",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "version": "node version-bump.mjs && git add manifest.json versions.json"
  },
  "keywords": ["obsidian", "plugin", "notifications", "reminders", "date", "tags"],
  "author": "Alexander Stolz",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^24.5.1",
    "builtin-modules": "^5.0.0",
    "esbuild": "^0.25.9",
    "obsidian": "^1.8.7",
    "tslib": "^2.8.1",
    "typescript": "^5.9.2"
  }
}
